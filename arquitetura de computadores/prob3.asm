# Desvia o fluxo para o byte 16 para proteger a região das Words de entrada/saída
JMP INICIO_PROG  

# --- ESPAÇO RESERVADO ATÉ O BYTE 16 ---
CLEAR
CLEAR
CLEAR
CLEAR
CLEAR
CLEAR
CLEAR

INICIO_PROG:
CLEAR
STORE 100        # [100] = ACUMULADOR DO PRODUTO = 0

LOAD 8           # Lê o endereço físico real da Word 2 (Vetor A)
STORE 110        # [110] = Vetor A vivo

LOAD 12          # Lê o endereço físico real da Word 3 (Vetor B)
STORE 120        # [120] = Vetor B vivo

# --- PROCESSA COMPONENTE 0 ---
LOAD 110
GET_BYTE
STORE 111
LOAD 120
GET_BYTE
MOVXY
LOAD 111
MUL
ADD 100
STORE 100

# --- PROCESSA COMPONENTE 1 ---
LOAD 110
SHR_BYTE
STORE 110
LOAD 120
SHR_BYTE
STORE 120
LOAD 110
GET_BYTE
STORE 111
LOAD 120
GET_BYTE
MOVXY
LOAD 111
MUL
ADD 100
STORE 100

# --- PROCESSA COMPONENTE 2 ---
LOAD 110
SHR_BYTE
STORE 110
LOAD 120
SHR_BYTE
STORE 120
LOAD 110
GET_BYTE
STORE 111
LOAD 120
GET_BYTE
MOVXY
LOAD 111
MUL
ADD 100
STORE 100

# --- PROCESSA COMPONENTE 3 ---
LOAD 110
SHR_BYTE
STORE 110
LOAD 120
SHR_BYTE
STORE 120
LOAD 110
GET_BYTE
STORE 111
LOAD 120
GET_BYTE
MOVXY
LOAD 111
MUL
ADD 100
STORE 100

# --- SALVAMENTO E FINALIZAÇÃO ---
LOAD 100
STORE 4          # Escreve no endereço físico real da Word 1 (Sucesso!)
HALT